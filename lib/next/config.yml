---
next:
  logger: stdout
  stdout_log_level: warn
  debug:
    receive: off
    autoreceive: off
    unhandled: off
    lifecycle: off

  actor:
    # Timeout (in seconds) for blocking actor initialization.
    creation_timeout: 3
